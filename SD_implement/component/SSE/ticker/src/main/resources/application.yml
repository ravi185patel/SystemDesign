server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27017/Pnstock
      auto-index-creation: true

logging:
  level:
    org.springframework.data.mongodb.core.ReactiveMongoTemplate: DEBUG
    org.mongodb.driver: DEBUG